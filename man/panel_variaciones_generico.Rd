% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_variaciones_generico.R
\name{panel_variaciones_generico}
\alias{panel_variaciones_generico}
\title{Panel de variaciones de precios (genérico)}
\usage{
panel_variaciones_generico(
  datos,
  calendarios,
  grupos = NULL,
  nombres_display = NULL,
  fecha_referencia = Sys.Date(),
  max_tickers_por_panel = 15,
  titulo = "Panel de Variaciones",
  nota_pie = NULL,
  cal_fallback = "cal",
  server = NULL,
  port = NULL
)
}
\arguments{
\item{datos}{data.frame con columnas \code{date}, \code{ticker}, \code{price}.
\code{date} debe ser convertible a \code{Date}.}

\item{calendarios}{Mapeo ticker -> calendario. Puede ser:
(i) named character vector con nombres de calendario \pkg{bizdays}, o
(ii) data.frame con columnas \code{ticker} y \code{calendario}.}

\item{grupos}{(Opcional) Mapeo ticker -> grupo. Puede ser named vector o
data.frame con columnas \code{ticker} y \code{grupo}. Si falta, usa \code{"OTROS"}.}

\item{nombres_display}{(Opcional) Mapeo ticker -> nombre para mostrar. Puede ser
named vector o data.frame con columnas \code{ticker} y \code{nombre}.}

\item{fecha_referencia}{Fecha base para el cálculo (por defecto \code{Sys.Date()}).}

\item{max_tickers_por_panel}{Máximo de filas por panel antes de dividir (default 15).}

\item{titulo}{Título a insertar como header en cada \pkg{flextable}.}

\item{nota_pie}{Nota al pie a insertar en cada \pkg{flextable}.}

\item{cal_fallback}{Nombre del calendario \pkg{bizdays} a usar si falta el específico.}

\item{server}{Host/alias para obtener feriados via \code{functions::getFeriados()}.}

\item{port}{Puerto para obtener feriados via \code{functions::getFeriados()}.}
}
\value{
Un objeto \code{flextable} si el panel entra en un solo bloque, o una lista
de \code{flextable} si requiere división.
}
\description{
Calcula variaciones de precios por ticker (1D, 5D, 1M, YTD) usando calendarios
de días hábiles por ticker, agrupa instrumentos y devuelve una o varias
\pkg{flextable} en función de \code{max_tickers_por_panel}.
}
\details{
Las fechas de referencia se calculan en días hábiles usando el calendario asignado
al ticker. Si el calendario no existe, se usa \code{cal_fallback}. Si \code{cal_fallback}
no existe, se crea con feriados de Argentina obtenidos desde \code{functions::getFeriados()}.
}
\examples{
\dontrun{
datos <- data.frame(
  date = as.Date(c("2026-01-02","2026-01-03")),
  ticker = c("AL30","AL30"),
  price = c(100, 101)
)
calendarios <- c(AL30 = "cal")
ft <- panel_variaciones_generico(datos, calendarios)
}

}
